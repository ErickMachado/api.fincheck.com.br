services:
  postgres:
    container_name: 'fincheck-pg'
    image: postgres:18.6-alpine3.24
    environment:
      POSTGRES_USER: '${POSTGRES_USER}'
      POSTGRES_PASSWORD: '${POSTGRES_PASSWORD}'
      POSTGRES_DB: '${POSTGRES_DATABASE}'
    ports:
      - '${POSTGRES_PORT}:5432'
