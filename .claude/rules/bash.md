# Bash

## Execução de comandos

- SEMPRE dê preferência para executar comandos no terminal usando o `rtk` (ex: `rtk ls -l`). Caso o `rtk` não esteja disponível, faça o fallback para versão normal (ex: `ls -l`);
